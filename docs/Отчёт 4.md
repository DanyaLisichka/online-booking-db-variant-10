# Отчёт

# Описание предметной области

В данной работе разрабатывается уровень доступа к данным для уже созданной базы данных онлайн-записи. Основная задача — реализовать объектно-ориентированную работу с MySQL через PDO, создать CRUD-операции, обеспечить безопасность SQL-запросов и продемонстрировать работу репозиториев.

Структура репозитория:

<p align="left"><img src="https://github.com/DanyaLisichka/online-booking-db-variant-10/blob/main/docs/images/Screenshot%202026-05-21%20at%2023.56.21.png?raw=true" alt="Структура"></p>


## Реализация подключения к базе данных

В файле config.php хранятся настройки подключения:
```
<?php

define('DB_HOST', 'localhost');
define('DB_NAME', 'psychology_center');
define('DB_USER', 'root');
define('DB_PASS', '');
define('DB_CHARSET', 'utf8mb4');
```

## Database.php

Реализуется Singleton для одного подключения PDO:
```
class Database
{
    private static ?PDO $instance = null;

    public static function getConnection(): PDO
    {
        if (self::$instance === null) {

            $dsn = "mysql:host=" . DB_HOST .
                   ";dbname=" . DB_NAME .
                   ";charset=" . DB_CHARSET;

            self::$instance = new PDO(
                $dsn,
                DB_USER,
                DB_PASS,
                [
                    PDO::ATTR_ERRMODE =>
                        PDO::ERRMODE_EXCEPTION
                ]
            );
        }

        return self::$instance;
    }
}
```

## Абстрактный репозиторий AbstractRepository.php

Создаётся общий базовый класс:
```
abstract class AbstractRepository
{
    protected PDO $pdo;

    public function __construct(PDO $pdo)
    {
        $this->pdo = $pdo;
    }

    abstract public function findAll();
    abstract public function findById(int $id);
    abstract public function delete(int $id);
}
```
  
## Репозитории таблиц
```
На примере ClientRepository для таблицы clients:
class ClientRepository extends AbstractRepository
{
    public function findAll()
    {
        $sql = "SELECT * FROM clients";

        return $this->pdo
            ->query($sql)
            ->fetchAll(PDO::FETCH_ASSOC);
    }

    public function findById(int $id)
    {
        $sql = "
            SELECT * FROM clients
            WHERE client_id = :id
        ";

        $stmt = $this->pdo->prepare($sql);

        $stmt->execute([
            ':id' => $id
        ]);

        return $stmt->fetch(PDO::FETCH_ASSOC);
    }

    public function delete(int $id)
    {
        $sql = "
            DELETE FROM clients
            WHERE client_id = :id
        ";

        $stmt = $this->pdo->prepare($sql);

        return $stmt->execute([
            ':id' => $id
        ]);
    }
}
```

Пример createAppointment()
```
public function createAppointment(
    int $clientId,
    int $psychologistId,
    string $date,
    string $type
)
{
    $sql = "
        INSERT INTO appointments
        (
            client_id,
            psychologist_id,
            appointment_date,
            consultation_type
        )
        VALUES
        (
            :client_id,
            :psychologist_id,
            :appointment_date,
            :consultation_type
        )
    ";

    $stmt = $this->pdo->prepare($sql);

    return $stmt->execute([
        ':client_id' => $clientId,
        ':psychologist_id' => $psychologistId,
        ':appointment_date' => $date,
        ':consultation_type' => $type
    ]);
}
```



## Безопасность

Используются только подготовленные выражения. Это защищает систему от SQL-инъекций.
```
$stmt = $pdo->prepare($sql);
$stmt->execute([
    ':id' => $id
]);
```

## demo.php

В demo.php демонстрируется:
- подключение к БД;
- создание объектов репозиториев;
- выполнение CRUD;
- вывод данных;
- обработка ошибок.

Фрагмент кода:
```
<?php


require_once 'config/config.php';
require_once 'src/Database.php';
require_once 'src/AbstractRepository.php';
require_once 'src/ClientRepository.php';


require_once 'src/PsychologistRepository.php';
require_once 'src/AppointmentRepository.php';
require_once 'src/RequestRepository.php';
require_once 'src/MethodRepository.php';

try {

    // Подключение к БД
    $pdo = Database::getConnection();

    echo "<h2>Подключение успешно</h2>";

    // Создание репозиториев
    $clientRepo = new ClientRepository($pdo);

    $psychologistRepo =
        new PsychologistRepository($pdo);

    $appointmentRepo =
        new AppointmentRepository($pdo);

    // Получение всех клиентов
    echo "<h3>Список клиентов:</h3>";
```

Результат: 
<p align="center"><img src="https://github.com/DanyaLisichka/online-booking-db-variant-10/blob/main/docs/images/Screenshot%202026-05-21%20at%2023.56.05.png?raw=true" alt="Результат"></p>


## Заключение

В ходе выполнения работы был разработан полноценный уровень доступа к данным для системы записи в психологический центр. Реализованы репозитории, CRUD-операции, защита от SQL-инъекций, транзакции и обработка ошибок. Проект соответствует требованиям объектно-ориентированного подхода и демонстрирует практическое применение PDO и паттерна Repository.

# Контрольные вопросы
## В чём заключается принцип единственной ответственности (Single Responsibility Principle) применительно к классам репозитория?

Автозагрузка классов в проекте организована через подключение файлов с помощью require_once. В файле demo.php последовательно подключаются необходимые классы и конфигурационные файлы:
```
require_once 'config/config.php';

require_once 'src/Database.php';
require_once 'src/AbstractRepository.php';

require_once 'src/ClientRepository.php';
require_once 'src/PsychologistRepository.php';
require_once 'src/AppointmentRepository.php';
```
При этом текущая реализация не является полноценной автозагрузкой по стандарту PSR-4, так как классы подключаются вручную, а не через Composer и пространства имён (namespace). В проекте реализована упрощённая схема загрузки классов, частично соответствующая принципам PSR-4, но без полноценной автоматической автозагрузки через Composer.